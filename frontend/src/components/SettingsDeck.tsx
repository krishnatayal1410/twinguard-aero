import{useEffect,useState}from"react";import{motion}from"framer-motion";import{Box,CheckCircle2,Database,Gamepad2,LogOut,Radio,RefreshCcw,ShieldCheck,Usb,UserRound}from"lucide-react";import{getSystemStatus}from"../services/twinApi";import type{SystemStatus}from"../types/twin";import{Badge,Card,SectionTitle,pretty}from"./ui";import{useAuthStore}from"../store/authStore";import{signOut}from"../services/authApi";
export default function SettingsDeck(){const[status,setStatus]=useState<SystemStatus>(),user=useAuthStore(s=>s.user),token=useAuthStore(s=>s.token),clear=useAuthStore(s=>s.clear);const load=()=>getSystemStatus().then(setStatus).catch(()=>setStatus(undefined));useEffect(()=>{void load()},[]);const items=[["Database",status?.database,Database],["MQTT",status?.integrations.mqtt?"Enabled":"Local / disabled",Radio],["CAN / SocketCAN",status?.integrations.can?"Enabled":"Adapter ready",Usb],["Unreal UDP",status?.integrations.unreal_udp?"Enabled":"Adapter ready",Gamepad2]]as const;
return <motion.div className="settings-page" initial={{opacity:0,y:8}} animate={{opacity:1,y:0}}><Card className="settings-hero"><SectionTitle eyebrow="SYSTEM INTEGRATION" title="TwinGuard platform status" action={<button className="icon-button" onClick={load}><RefreshCcw size={15}/></button>}/><div className="system-status-grid">{items.map(([n,v,Icon])=><div key={n}><Icon size={24}/><span>{n}</span><strong>{v??"Waiting…"}</strong><CheckCircle2 size={15}/></div>)}</div></Card><div className="settings-grid"><Card><SectionTitle eyebrow="OPERATOR ACCOUNT" title="Signed-in identity"/><div className="account-settings-card"><UserRound/><div><strong>{user?.name}</strong><span>{user?.email}</span><Badge kind="blue">{pretty(user?.role??"operator")}</Badge></div><button onClick={async()=>{await signOut(token);clear()}}><LogOut size={14}/>Sign out</button></div></Card><Card><SectionTitle eyebrow="AI MODEL PACK" title="Loaded predictive models"/><div className="status-list">{Object.entries(status?.models??{}).map(([k,v])=><div key={k}><span>{pretty(k)}</span><Badge kind={v?"good":"warn"}>{v?"Loaded":"Fallback"}</Badge></div>)}</div></Card><Card><SectionTitle eyebrow="SECURITY DEFAULTS" title="Local MVP hardening"/><div className="security-list"><div><ShieldCheck/><span>Operator sign-in and expiring sessions</span></div><div><ShieldCheck/><span>PBKDF2-SHA256 password hashing</span></div><div><ShieldCheck/><span>Restricted CORS origins</span></div><div><ShieldCheck/><span>Trusted host validation</span></div><div><ShieldCheck/><span>Telemetry ingest key</span></div><div><ShieldCheck/><span>Security response headers</span></div><div><ShieldCheck/><span>Loopback-only local services</span></div></div></Card><Card><SectionTitle eyebrow="3D / INTEROPERABILITY" title="Digital Twin bridges"/><div className="bridge-list"><div><Box/><span>glTF / GLB engine asset</span></div><div><Gamepad2/><span>Unreal Engine UDP JSON stream</span></div><div><Radio/><span>MQTT telemetry adapter</span></div><div><Usb/><span>python-can / SocketCAN adapter</span></div></div></Card></div><Card><SectionTitle eyebrow="DEPLOYMENT NOTE" title="Production boundary"/><p className="settings-note">This project is a synthetic engineering MVP. Before connection to a physical UAV engine, calibrate the physics and AI models using authorized engine/test-rig data, place the API behind authenticated network controls, use TLS, and validate all CAN/ECU mappings against official interface documentation.</p></Card></motion.div>}
