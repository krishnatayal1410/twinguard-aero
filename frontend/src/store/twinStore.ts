import{create}from"zustand";import type{MissionResult,ReplayMission,TwinState,ViewName}from"../types/twin";
interface Store{twin?:TwinState;online:boolean;view:ViewName;focus:string;history:Record<string,number[]>;missionRuns:Array<{fault:string;result:MissionResult}>;missions:ReplayMission[];setTwin:(x:TwinState)=>void;setOnline:(x:boolean)=>void;setView:(x:ViewName)=>void;setFocus:(x:string)=>void;addMission:(fault:string,r:MissionResult)=>void;setMissions:(m:ReplayMission[])=>void}
const keys=["rpm","cht","egt","oil_pressure","oil_temperature","vibration","altitude","battery_voltage"];
export const useTwinStore=create<Store>(set=>({online:false,view:"command",focus:"all",history:Object.fromEntries(keys.map(k=>[k,[]])),missionRuns:[],missions:[],setTwin:x=>set(s=>{const h={...s.history};for(const k of keys)h[k]=[...(h[k]??[]),Number(x.telemetry[k]??0)].slice(-90);return{twin:x,history:h}}),setOnline:online=>set({online}),setView:view=>set({view}),setFocus:focus=>set({focus}),addMission:(fault,result)=>set(s=>({missionRuns:[...s.missionRuns,{fault,result}].slice(-8)})),setMissions:missions=>set({missions})}));
